
function checkAnswer(selectedOption) {
  const correctAnswer = "A"; // Dette bør endres basert på det aktuelle spørsmålet

  if (selectedOption === correctAnswer) {
    alert("Riktig svar!");
  } else {
    alert("Feil svar, prøv igjen!");
  }
}
